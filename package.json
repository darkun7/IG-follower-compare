{
  "name": "ig-follower-compare",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --workspace=client",
    "dev:frontend": "npm run dev --workspace=client",
    "dev:backend": "python -m uvicorn server.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "client"
  ]
}
